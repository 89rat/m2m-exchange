{
  "name": "@m2m/protocol",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "M2M/1 commerce protocol types (protocol/PROTOCOL.md) — zero runtime dependencies",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
